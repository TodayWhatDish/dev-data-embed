# data/ — CSV 레이아웃

16테이블 스키마(`src/create_schema/`)에 넣을 값. **파일 1개 = 테이블 1개**이고
헤더는 DDL 의 컬럼명·순서 그대로다.

```
data/master/*.csv ┐
                  ├─[load_csv.py]─> user.db
data/seed/*.csv   ┘     ▲
      ▲                 └ animal_categories / product_categories / feeding_purposes
      └─[gen_seed.py]     는 여기 없다. 코드(*_schema.py 의 SEEDS)가 넣는다.
```

실행 순서:

```bash
py src/make_data/gen_seed.py   # data/seed/*.csv 재생성 (시드 고정)
py src/load_csv.py             # 스키마 재생성 + 적재 + 검증
```

**적재 순서는 손으로 적지 않는다.** 부모가 아직 없는 테이블에 INSERT 하면 FK 검증에 걸리므로
순서가 중요한데, 그 정답은 이미 DDL 안에 있다 — `PRAGMA foreign_key_list` 가 "이 테이블이
무엇을 참조하는가"를 알려주고, 그 그래프를 위상 정렬하면 순서가 나온다(`resolve_order()`).
`load_csv.py` 가 실행할 때마다 계산해서 근거와 함께 출력한다.

```
 1. allergens                     [자기참조]
 2. breeds                         (시드 참조: animal_categories)
 4. products                       (시드 참조: product_categories)
 7. pets                          <- users
13. pet_breeds                    <- breeds, pets
```

C블록(`purchases`/`reviews`)이 생기면 `SOURCES` 에 파일만 추가하면 된다 —
순서는 다시 계산되어 알아서 뒤에 붙는다.

---

## 세 갈래로 나눈 이유

| 갈래 | 무엇 | 누가 채우나 | 재생성 |
|---|---|---|---|
| **코드 SEEDS** | `animal_categories`, `product_categories`, `feeding_purposes` | `*_schema.py` | 스키마와 함께 |
| **`data/master/`** | `allergens`, `breeds`, `ingredients`, `ingredient_allergens` | 사람 | **하지 않는다** |
| **`data/seed/`** | 나머지 9테이블 | `gen_seed.py` | 언제든 |

**마스터를 재생성하지 않는 것이 이 구분의 핵심이다.** `allergen_id` 가 한 번 바뀌면
손으로 넣은 `ingredient_allergens` 매핑이 통째로 어긋난다 — 에러 없이, 조용히.
생성기는 마스터를 **읽기만** 한다.

코드 SEEDS 에 있는 3개를 CSV 로 또 빼지 않는 이유는 같은 값이 두 군데 앉기 때문이다.
행이 2~6개라 코드에 두는 비용이 없다.

---

## 파일

### data/master/ — 손으로 채운다

| 파일 | 행 | 담는 것 |
|---|---|---|
| `allergens.csv` | 49 | 알러지원 트리. 단백질/곡물/콩류/견과류/채소/첨가물 6뿌리, 최대 4단계 |
| `breeds.csv` | 36 | 개 23 + 고양이 13. `name_eng` 가 NULL 인 행(삽살개·풍산개)이 섞여 있다 |
| `ingredients.csv` | 57 | 원료 표기. `닭고기`·`닭가슴살`·`계육분`·`닭지방` 이 따로 있다 |
| `ingredient_allergens.csv` | 56 | 그 4개를 전부 `닭고기` 알러지원 하나로 잇는다. 문자열 매칭을 없애는 표 |

**`ingredients` 57행 중 10행에 알러지원이 없다** (단호박·스피루리나·글루코사민·
프로바이오틱스·유산균·오메가3·비트펄프·아마씨·크랜베리·타우린). 매핑을 빠뜨린 게 아니라
실제로 알러지원이 아니다. 다만 `ingredient_allergens` 0행은 `v_product_safety` 에서
'알러지원 없음'으로 읽히므로(2026-08-24 에 `allergen_reviewed` 를 뺀 뒤의 fail-open),
**원료를 추가할 때 매핑을 같이 넣지 않으면 그 원료가 든 제품이 `Safe` 로 통과한다.**

`allergens.csv` 의 행 순서는 지켜도 되고 안 지켜도 된다 — 로더가 `parent_id` 를 보고
부모부터 넣도록 정렬한다. 순환 참조는 그 자리에서 거부한다.

### data/seed/ — gen_seed.py 가 뽑는다

| 파일 | 행 |
|---|---|
| `users.csv` | 300 |
| `pets.csv` | 368 |
| `pet_breeds.csv` | 417 |
| `pet_allergies.csv` | 468 |
| `products.csv` | 200 |
| `product_animal_categories.csv` | 209 |
| `product_nutrition.csv` | 159 |
| `product_feeding_purposes.csv` | 220 |
| `product_ingredients.csv` | 931 |

시드는 `20260812` 고정, 데이터의 '오늘'은 `2026-08-25` 고정이다. `date.today()` 를 쓰면
재실행할 때마다 나이·휴면 판정이 흔들려서 벤치를 비교할 수 없다.

분포는 스키마 문서의 서술과 도메인 상식으로 정한다. **옛 `data/pet_*.csv` 4종은 참고하지 않는다.**

`schema/` 문서는 "어떤 컬럼이 왜 있는가"를 적은 것이지 "무슨 값을 넣어라"가 아니다.
그래서 아래 값들은 **이 생성기가 정한 것**이고 실측 통계가 아니다 — 바꿀 때 죄책감 가질 것 없다.

| 정한 값 | 근거 |
|---|---|
| 품종 0행 (개 8% / 고양이 4%) | 고양이는 '코숏'이라는 포괄 명칭이 있어 '모름'이 개보다 드물다 |
| 고양이 품종 가중치 (코숏 45) | 국내 반려묘 구성상 코숏이 압도적이어야 한다. 개는 근거가 없어 균등 |
| 성분표 보유율 (사료 98% / 간식 55~65%) | 사료관리법상 사료는 표시 의무. 간식은 아니다 |
| `ingredients_verified` 78% | `None`(판정불가) 분기가 관측될 만큼 |
| 제형별 100g당 단가 | 제형 간 가격차의 방향만 맞췄다 |

---

## CSV 규약

- **다중값 컬럼을 만들지 않는다.** 옛 `pet_products.ingredients` 의 `'닭고기|현미|오메가3'`
  같은 파이프 구분은 연결 테이블 행으로 푼다. 정규화한 이유가 그것이다.
- **NULL 은 빈 칸.** 로더가 `'' -> None` 으로 바꾼다.
- **UTF-8, BOM 없음, 개행 LF.** 옛 `data/pet_*.csv` 4종에는 BOM 이 붙어 있으니 섞지 않는다.
- **값 캐스팅을 로더가 하지 않는다.** STRICT 테이블이 `'30000'` 은 INTEGER 로 받고
  `'삼만원'` 은 거부한다. 파이썬에서 `int()` 를 먼저 부르면 같은 검사를 두 번 하는 셈이다
  (근거: `execute_schema.py` 설계규칙 7번).

---

## DB 가 막지 못해 생성기가 책임지는 것

`docu/schema/` 가 "앱이 막는다"로 적어둔 것들이다. 데이터를 손으로 고치거나 생성기를
바꿀 때 여기서 어긋나면 **에러 없이 조용히 틀린다.**

**① `pet_allergies` 펼침** — 카테고리를 고르면 하위를 전부 넣고 **고른 카테고리 행도 남긴다.**
`가금류` 를 고른 개체는 `가금류`/`닭고기`/`오리`/`칠면조` 4행이다. 카테고리 행이 있어야
나중에 `거위` 가 추가됐을 때 대상을 찾을 수 있다.

**② 축종 정합성** — 개 `pet` 에 고양이 `breed` 를 붙여도 FK 는 통과한다. 생성기가
`BREEDS_BY_CATEGORY` 로 막는다. `product_animal_categories` 도 같다.

**③ 판정 3분법이 실제로 관측되도록 분포를 섞는다.** `ingredients_verified = 0` 인 제품과
`product_animal_categories` 0행 제품을 일부러 남긴다. 전부 `1` / 전부 1행이면
`v_product_safety` 의 `None`(판정불가) 분기가 한 번도 안 나와서, 스키마는 멀쩡한데
검증만 헛돈다. 현재 실측:

```
v_product_safety 전체: Safe 32446쌍 / None 7492쌍 / WARN 4438쌍
활성 펫 358마리 중 Safe 후보 0개인 펫 6마리
```

**Safe 후보 0개인 6마리는 버그가 아니다.** 여섯 다 `단백질` 최상위 노드를 골라
육류·어류·갑각류·유제품·난류가 통째로 배제된 개체다(알러지 25~26행). 실제로 일어나는
상황이고, 이 경우 앱이 무엇을 보여줄지는 아직 정하지 않았다.

`load_csv.py` 가 적재 후 위 두 줄을 매번 출력한다. 한 종류로 쏠리면 경고한다.

---

## 문서가 요구한 케이스를 데이터가 담고 있는가

**스키마가 어떤 구조를 고른 데는 이유가 있고, 그 이유는 대개 "이런 경우가 있으니까"다.**
그 경우가 데이터에 하나도 없으면 구조는 있는데 한 번도 시험되지 않는다. `docu/schema/` 의
4개 문서에서 그런 진술을 뽑아 실측한 결과다.

| 문서 | 문서가 말한 것 | 데이터 |
|---|---|---|
| `product_schema.md` | 복합 원료는 알러지원을 여러 개 갖는다 — `'베이커리 부산물'`은 밀·계란·유제품 | 복합 원료 **5종** |
| `product_schema.md` | 소형견 전용 = `target_size_min 2, max 2` | `min == max` 제품 **15개** |
| `product_schema.md` | `description` 은 후기 없는 신규 제품(콜드스타트)의 임베딩 대상 | 축종·체구·연령·주원료·목적을 한 문장에 |
| `user_schema.md` | 같은 uid 라도 제공자가 다르면 다른 계정이다 | 제공자만 다른 uid **15쌍** |
| `user_schema.md` | `phone` 에 UNIQUE 를 안 건다 — 가족 공유, 번호 재할당 | 중복 번호 **14개** |
| `common_schema.md` | `allergens.name_eng` — 영문명. 없으면 NULL | NULL **2행**(도라지·더덕) |
| `pet_schema.md` | `breeds.name_eng` 가 NULL 인 품종이 공존한다 | NULL **2행**(삽살개·풍산개) |
| `pet_schema.md` | `pet_breeds` 0행 = 모름 / 2행 이상 = 믹스 | 0행 **35** / 2행+ **78** |
| `product_schema.md` | `product_nutrition` 은 결측 때문에 분리했다 | 성분표 없는 제품 **41개** (거의 전부 간식) |

**복합 원료가 이 중 제일 중요하다.** `ingredient_allergens` 가 컬럼 하나가 아니라 다대다
테이블인 **유일한 이유**가 이것이고, 전부 1:1 매핑이면 컬럼 하나로 둔 설계와 결과가 같아진다.
실제로 지금 데이터에서 **복합 원료 때문에 위험 판정된 쌍이 1,400여 개**다 — 컬럼 하나였다면
그중 상당수가 조용히 `Safe` 로 통과했다.

```
베이커리부산물 -> 우유, 계란, 밀
가금부산물분   -> 닭고기, 오리, 칠면조
어분           -> 연어, 참치, 대구
혼합곡물분     -> 밀, 보리, 옥수수
동물성유지     -> 닭고기, 소고기
```

## 조인했을 때 앞뒤가 맞는가

행 하나만 보면 멀쩡한데 **두 값을 같이 보면 틀린** 것들이다. FK 도 CHECK 도 못 잡는다.
`gen_seed.py` 가 값을 뽑는 순서로 막는다 — 뒤에 오는 사건은 앞선 사건을 하한으로 삼는다.

| 규칙 | 왜 |
|---|---|
| `users`: 가입 → 로그인 → 탈퇴 → 수정 | **탈퇴한 뒤에는 로그인할 수 없다.** 그래서 `withdrawn_at` 은 `last_login_at` 이후로만 뽑는다 |
| `pets`: max(생일, 주인 가입) → 등록 → 활동종료 → 수정 | 하한이 **두 개**다. 주인 가입일만 보면 태어나기 전에 등록된 개체가 생긴다 |
| `products`: 등록 → 수정 | 둘을 따로 뽑으면 절반이 '3년 전에 수정됨'이 된다 |
| 100g당 가격 = `price_krw` / `weight_g` | 가격을 중량과 따로 뽑으면 이 파생값이 무의미해진다. **중량을 먼저 뽑고 단가를 곱한다** |

**앱에서 일어날 수 있는 일만 만든다.** 컬럼 값이 각각 유효해도 **행위의 선후**가 안 맞을 수 있다.
펫 등록은 앱에 로그인해야 하는 일이라, `last_login_at` 이 그 상한이 된다.

| 규칙 | 예전에 뭐가 나왔나 |
|---|---|
| `last_login_at` 이 NULL 이면 펫이 없다 | 한 번도 로그인 안 한 유저 14명 **전원**이 펫을 갖고 있었다 |
| 펫 등록·수정 ≤ 마지막 로그인 | 마지막 로그인 **이후**에 찍힌 활동이 96건 |
| 펫 등록 ≤ 탈퇴 시각 | 탈퇴한 계정이 펫을 등록한 셈이 됐다 |
| 한 집에 같은 이름 두 마리는 없다 | 이름 풀이 40개라 실제로 5쌍이 겹쳤다 |
| 가입만 하고 펫을 안 올린 유저가 있다 | 300명 **전원**이 펫을 갖고 있었다 (지금 25명이 0마리) |

마지막 줄이 특히 중요하다 — '펫 0마리 유저'가 없으면 그 상태를 앱이 어떻게 다루는지
시험할 수 없다. 다른 0행 케이스들과 같은 이유다.

**마스터에 있는 알러지원은 원료로도 존재해야 한다.** `땅콩` 알러지를 등록했는데 `ingredients` 에
땅콩이 없으면 **걸러지는 제품이 0개**가 된다 — 하드 필터가 조용히 무의미해진다.
예전에 게·땅콩·호밀·수수·곤충단백·인공색소·인공보존료 7종이 그 상태였고, 그 알러지를 등록한
개체가 72마리였다. 원료를 채워서 메웠다.

남겨둔 것은 `도라지`·`더덕` 둘뿐이다 — 한방 간식에만 쓰여서 현재 취급 제품에 없는 게 자연스럽고,
'마스터에는 있지만 아무 제품에도 안 들어가는 알러지원'이라는 상태도 하나쯤 있어야 한다.
(중간 노드 15개가 원료를 직접 안 가리키는 것은 정상이다 — 자식이 가리킨다.)

**결측은 분류를 보고 낸다.** 예전에는 성분표 유무를 분류와 무관하게 일괄 85% 로 정해서
사료 113개 중 14개가 성분표 없이 나왔다. 사료관리법상 배합사료는 등록성분량 표시가 **의무**라
그런 사료는 유통될 수 없고, `product_schema.md` 도 결측의 방향을 명시해 뒀다 —
"**사료는** 성분표가 있고 **간식은** 없는 경우가 흔하다".

| 분류 | 성분표 결측 |
|---|---|
| 사료 | **1% 안팎** |
| 덴탈껌 · 수제간식 | 40% 안팎 |
| 트릿 | 20~35% |

결측 자체는 남긴다 — 그게 이 표를 `products` 에서 분리한 이유다. 쏠리는 방향만 바로잡았다.

**보증성분 5개의 합은 100% 를 넘지 않는다.** 나머지는 탄수화물(가용무질소물) 몫이라
최소 1% 를 남긴다. 습식은 수분이 80% 대라 그냥 뽑으면 합이 104% 까지 나온다 —
사료 쪽 결측을 줄이자 성분표가 늘면서 이 잠복 결함이 드러났다.

**시각까지 순서를 맞춘다.** 날짜만 뽑고 시:분:초를 나중에 무작위로 붙이면 같은 날일 때
`09:00` 가입 → `03:00` 로그인이 나온다. `rdt()` 가 초 단위로 뽑아서 이 구멍을 없앤다.

실측 (2026-08-26 기준, 전부 0건이어야 한다)

```
시간 역전 14종 검사 ....... 0건
미래 시각(2026-08-25 초과) . 0건
보증성분 합계 100% 초과 ..... 0건
제형 vs 수분 역전 .......... 0건
```

100g당 가격은 제형 안에서 **1.6~3.6배**로 좁혀졌고, 제형 사이 차이(건식 738원 ↔ 동결건조
9,412원)는 실제 시장과 같은 방향이다. 대용량 할인도 걸린다 — 건식 사료 7kg↑ 610원 /
1~3kg 958원.

---

**이 표는 스키마 문서를 고칠 때 같이 본다.** 새 설계 노트가 "이런 경우가 있어서 이렇게 했다"고
말하면, 그 경우가 데이터에 있는지 여기에 한 줄 추가해서 확인한다.

---

## 아직 없는 것

**구매·리뷰(C블록)** — `docu/schema/README.md` 기준으로 아직 새 스키마에 포팅되지 않았다.
옛 `pet_purchases.csv` (1,439건, 별점에 매칭 신호가 심긴 `DATAINFO.md` §4.1)에 해당하는
CSV 는 그 테이블이 생긴 뒤에 만든다. `src/embed.py` 의 리뷰 임베딩도 그때까지는 옛 경로다.

## 옛 파일

`data/pet_customers.csv`, `pet_profiles.csv`, `pet_products.csv`, `pet_purchases.csv` 4종은
**옛 4테이블 비정규화 스키마**의 것이다. `src/load_db.py` 만 읽는다. 설명은 `docu/DATAINFO.md`.
값 분포의 재료로는 아직 쓰이지만, 새 파이프라인은 이 파일들을 읽지 않는다.
