서비스 선정 이유, 비즈니스 활용방식 (어떤 서비스와 비슷했는지, 왜 애완동물로 설정했는지(도메인이 깊었음), B2C 방식으로 어떻게 활용할건지, 토큰관리 GOAL.md 참조)
데이터 선정 기준과 스키마 설계 (스키마 설계와 DB 테이블 구성 기준과 설계방식)
데이터 마스킹과정과 평가방식(eval,test) (pytest를 사용하여 청킹,임베딩 품질을 확인하고 데이터 마스킹 품질 측정을 시각화)
관리자 대시보드 설계와 반증 처리 (반증 방식을 뭐로 도입했고, 어떻게 동작하는지 왜 LLM이 LLM을 반증평가했으며, 서로다른 자사의 모델이 아닌 Antrophic끼리 했는지)
데이터 변동에 대한 임베딩과 벡터라이즈 (서비스 도중 db에 새로운 리뷰데이터가 적재될 경우 어떻게 캐시된 내용 이외로 업데이트하는지)
fastapi 선정 이유와 라우터 동작방식 (flask, mjango를 쓰지않고 fastapi를 선정한 이유는 뭔지, web과 라우터 기능을 어떻게 pingpong하는지 )
langchain 활용과정과 상용api선정이유와 근거 (로컬모델과 얼만큼의 지표가 차이 났는지? openAI를 안쓰고 antrophic를 선정한 이유는 뭔지?)

모든 주제에 대해서는 근거와 지표를 기반으로 설명할 것.
필요한 주제에 대한 서비스 화면 캡쳐사진 첨부

자료 생성시 토큰이 적게 드는 방향으로 만들것 (pptx vs html)


제목 : 오늘뭐먹냥
부제목 : 우리아이의 소중한 한입
팀원 이름 허재성,박승호,최우진,한인혁,최종명

1. 서비스 선정이유와 비즈니스모델(BM) : 즉, 돈이 되는 도메인인가?

