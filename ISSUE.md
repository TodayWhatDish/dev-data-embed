# ISSUE.md — 관리자 대시보드 세션 정리

이번 세션에서 다룬 건 크게 두 갈래: (1) `/ask` 답변의 **반증(팩트체크)** 기능 추가, (2) 관리자
대시보드(`web/admin.html`, `web/admin.js`) UI/UX 정리·리디자인. 아래는 이슈 단위로 문제 → 고민한
지점 → 해결 순으로 정리.

---

## 1. LLM 답변을 어떻게 검증할 것인가 (반증/팩트체크)

**문제**: `/ask`가 자유 텍스트로 답을 만드는데, 이 답변이 실제 고객 구매 이력과 맞는 말인지 보장할
방법이 없었다. (기존 `Strategy`/`Citation` 기능은 구조화 출력이라 SQL로 대조 가능했지만, 자유 텍스트
답변은 그런 스키마가 없다.)

**고민한 지점**: 답변을 만든 모델이 자기 자신을 채점하게 두면 의미가 없다 — 틀린 답을 만든 모델이
그 틀린 답을 "맞다"고 채점할 수 있다.

**해결**: `app/domain/prompting.py`에 `FactCheck` 구조화 출력 모델(`accuracy`, `note`)과
`build_factcheck_prompt()`를 추가하고, `app/features/answering.py`의 `verify()`가 **답변을 만든
호출과 별도로** `chat.with_structured_output(FactCheck)`를 호출해 `[고객 정보]`와 대조한다
(LLM-as-judge 패턴). `/ask`의 NDJSON 스트림에 `verification` 청크로 얹어서 내려보낸다
(`app/api/routes/ask.py`).

- 알려진 미해결 포인트: 구매 이력이 아예 없는 고객(`구매 이력 없음`)에도 매번 `verify()`가 LLM을
  한 번 더 호출한다. 호출량이 문제되면 이 케이스는 스킵하도록 조기 반환을 추가할 것.

## 2. 반증 결과와 원본 구매 이력을 어떻게 구분해서 보여줄 것인가

**문제**: "반증 출력은 빨간 글씨로" 요청을 처음엔 잘못 해석해서, 원본 구매 이력(`#askFacts`,
사실 그 자체)까지 빨간색으로 칠했다. 사용자가 "구매이력이 왜 빨간색으로뜸?"이라고 지적.

**고민한 지점**: 빨간색은 "검증 결과가 안 좋다"는 신호여야지, 사실 자료 자체에 씌우면 오히려
사실을 의심하게 만든다.

**해결**: 빨간색(`color:#c0392b`)은 검증 **판정**(`#askVerify`)에만 남기고, 원본 구매 이력
(`#askFacts`)은 다른 인용/근거 텍스트와 같은 중립 회색(`.review-text`)으로 되돌렸다.

## 3. 정확도 점수만 있고 근거 설명이 없음

**문제**: `FactCheck.note`가 "일치" 한 단어로 끝나는 경우가 있어 왜 그 점수가 나왔는지 알 수 없었다.

**해결**: `note` 필드의 `Field(description=...)`을 "답변의 어느 부분이 [고객 정보]의 어느 내용과
일치/불일치하는지 구체적으로 짚고, 100%가 아니면 왜 깎였는지 반드시 밝히고, 100%여도 근거를
한두 문장 적으라"고 명시적으로 강제했다.

## 4. 개인정보(전화번호)가 프롬프트에 새는지 확인

**문제**: LLM이 전화번호를 답하지 않는 걸 보고 "마스킹이 잘된 거겠지?"라는 질문이 나왔는데, 실제로는
"마스킹" 로직이 코드 어디에도 없었다 — 확인 없이 넘어갈 사안이 아니었다.

**고민한 지점**: 짐작으로 답하지 않고 코드로 확인.

**해결**: `app/repositories/users.py`의 `get_user_detail()`은 `phone`을 조회해 `detail` 딕셔너리에
넣지만, `build_customer_context()`/`build_strategy_prompt()`(`app/domain/prompting.py`)는
`detail["purchases"]`, `detail["name"]`, `detail["region"]`처럼 **필요한 키만 선택적으로 꺼내 쓴다** —
`phone`/`email`은 애초에 프롬프트에 들어갈 경로가 없다. 이건 명시적 마스킹이 아니라 "선택 목록 기반
구조적 누락"이라 **`build_customer_context()`가 나중에 `detail` 전체를 덤프하는 식으로 바뀌면
깨질 수 있는 약한 지점**이라는 걸 기록해둔다.

## 6. 사이드 패널에 모든 게 몰려서 UX가 무너짐

**문제**: 회원 정보, DB/API 연결 상태, 임베딩 검증 결과를 전부 좁은 AI 사이드 패널 하나에 넣으려다
보니 스크롤이 끝없이 길어지는 구조가 됐다.

**고민한 지점**: 두 종류의 관심사를 섞어 쓰고 있었다 — (a) "이 고객"에 대한 AI 기능(추천/전략/질문)과
(b) 고객과 무관한 시스템 전역 정보(DB 상태, 임베딩 검증). 하나의 패널에 넣을 이유가 없다.

**해결**:
- (a)는 AI 패널 안에서 **탭**(추천/전략/질문)으로 나눔 (`.ai-tabs`).
- (b)는 상단바의 **뷰 스위처**(회원/검증/시스템, `#viewSwitch`)로 분리 — `.full-view` + `hidden`
  속성으로 한 번에 하나의 화면만 보이게 함 (`switchView()`, `web/admin.js`).

## 7. 임베딩 품질 검증을 지금 다 구현할 것인가

**문제**: "임베딩 검증결과 시각화"를 요청받아 `pipeline.eval.eval`이 이미 만들어둔
`data/eval/*.json`을 읽는 `/api/eval` 엔드포인트 + Chart.js 막대그래프까지 구현했는데, 사용자가
"나중에 `tests/eval/`에서 pytest로 검증할 거다, 지금은 자리만 만들어달라"고 정정.

**고민한 지점**: 이미 동작하는 기능을 만들었어도, 방향이 다르면 지우는 게 맞다 — 나중에 pytest 결과
포맷과 지금 만든 JSON 포맷이 다를 게 뻔해서 유지하면 이중 관리가 된다.

**해결**: `/api/eval`과 관련 import(`json`, `Depends`, `get_current_admin`, `EVAL_DIR`)를
`app/api/routes/health.py`에서 전부 제거. 프론트도 fetch/차트 로직을 걷어내고 `#verifyView`에
"준비 중입니다" 안내문만 남김. 참고할 레퍼런스(`docs/REFERENCES.md`의
`C:\rag-project-cleanup\rag-project-cleanup`, `C:\axi-rag-deploy`)는 **이 환경에 실제로 존재하지
않아 못 열어봤다는 사실을 그대로 `TODO.md`에 기록**(추측으로 구조를 지어내지 않음).

- **알려진 불일치**: `TODO.md`가 이후 리팩터링(`#evalPanel` → `#verifyView`, `openEvalPanel()` →
  `switchView("verify")`)을 반영하지 못하고 옛 이름을 그대로 참조하고 있다. 다음에 `tests/eval/`
  작업을 시작할 때 `TODO.md`를 먼저 최신 구조로 고칠 것.

## 8. 대시보드를 "이쁘게" 꾸미기 — `/design` 스킬 부재

**문제**: 사용자가 "`/design` 스킬 써서 강아지/고양이 테마로 꾸며달라"고 요청했는데, 이 환경엔
`/design`이라는 스킬이 설치돼 있지 않았다.

**고민한 지점**: 없는 스킬을 있는 척 부르지 않고, 먼저 확인부터 한다 — 프로젝트 로컬
(`.claude/skills`: `accelation-mode`, `education-mode`, `pet-reco`)과 전역
(`~/.claude/skills`: `graphify`) 둘 다 확인.

**해결**: 없다는 사실을 사용자에게 알린 뒤, 직접 CSS/마크업으로 리디자인 진행. Google Fonts
"Baloo 2"로 타이틀류 폰트 교체, 로그인 카드형 UI, 카드 그림자, pill 버튼, hover 인터랙션,
개/고양이 이모지(당시 기준) 추가.

**후속 조정** (다음 요청에서): "아이콘이 너무 AI 티 난다"는 피드백을 받고, 장식용으로 뿌려둔
🐾/🍚/🦴/🔍/⚙️ 이모지를 대부분 제거하고 `petEmoji()`는 정보를 실제로 대체하는 곳(`avatar-lg`)에만
남겼다 — 텍스트 라벨과 중복되는 이모지(태그, 사이드바 종 표시)는 빼서 장식과 정보를 구분했다.

## 10. 줄바꿈이 뭉개져서 안 나옴

**문제**: `.review-text`에 `white-space: pre-wrap`이 빠져 있어서, 여러 줄로 조립된 텍스트
(구매 이력 요약, 리뷰 본문 등)가 브라우저 기본 동작(`white-space: normal`)대로 줄바꿈이 공백으로
뭉개졌다.

**해결**: `.review-text` 클래스 자체에 `white-space: pre-wrap` 추가(개별 인라인 스타일로 반복하지
않고 한 곳에서 해결). `#askVerify`(반증 결과)에도 동일하게 적용.

## 11. 질문 탭의 정보 순서가 뒤바뀜

**문제**: 화면에 실제 구매 이력이 답변보다 먼저 나와서, 사용자가 "질문에 대한 답이 아니라 구매
이력부터 보인다"고 지적.

**해결**: `data-panel="ask"` 안의 순서를 답변(`#askAnswer`) → 반증(`#askVerify`) → 그 아래
`.scroll-block`(최대 높이 170px, 내부 스크롤)로 압축한 실제 구매 이력(`#askFacts`)/임베딩 검색
결과(`#askSources`) 순으로 재배치. 참고 자료는 있어야 하지만 답변을 밀어내면 안 된다는 우선순위를
레이아웃에 반영.
